Claude，请读取以下两个文件：
1) ai_coordinating_protocol.md
2) project_process.md

执行规则（严格遵守 protocol）：
- 行处理优先级（从上到下，稳定顺序）：
  1）优先处理紧急区。如果紧急区没有plan（或者紧急区不存在），或者紧急区全部内容都已经打上✅，再执行正常表格计划的任务。
  1) 先处理标记为「❓」的行（修正 Codex 提出的疑虑）
  2) 再处理标记为「⏳」的行（继续未完成的实现）
  3) 最后处理空状态的行（新开工）
- 每轮最多处理 3 行；同一行只在本轮内提交一次修改。

对每一行，完成后请回填：
1) 在 “claude process” 栏：
   - 列出修改过的文件路径/模块
   - 关键实现步骤（要点式）
   - 使用的命令（如编译/测试指令）
2) 在 “claude comment” 栏：
   - 给出本次改动的验证方法（如何复现）
   - 关键输出/快照（如运行日志要点、生成的文件名）
   - 若属于修复❓的行，点对点回应 Codex 的具体意见（逐条说明如何解决）
3) 在该行状态末尾追加：
   - ⏳（仍在进行）或 ✅（功能完成）

注意：
- 不要改动 “codex comment” 栏的原始内容（除非按协议需要在 comment 中逐条回应）。
- 若遇到阻塞（外部依赖/信息不足），在 “claude comment” 末尾写明「Blocker: …」。

本轮全部完成后，请在回复末尾给出「执行摘要」：
- 影响的文件/模块清单
- 已解决的❓列表与对应修复点
- 仍未完成的行与下一步建议
然后等待 Codex 审核。
