Codex，请读取以下两个文件：
1) ai_coordinating_protocol.md
2) project_process.md

审核范围（从上到下）：
- 审查所有 “claude process / claude comment” 已填写且状态为「❓」或「⏳/✅」的行。
- 对「❓」行优先审核，随后审核「⏳/✅」。

审核要求（必须做“Claude comment ↔ 实际代码”的逐项比对）：
1) 按行对照：
   - 将 “claude comment” 中承诺/描述的行为，逐文件核对到实际代码实现：
     * 文件是否存在、路径是否正确
     * 关键函数/类/配置是否新增或修改（签名、参数、默认值）
     * 阈值/开关是否从 YAML/ENV 注入而非硬编码
     * 冲突/门控/冷却等规则是否按蓝图落地
     * 日志/telemetry/快照是否按描述产出
   - 若为修复❓的行，核对 Claude 是否逐条回应并真正修掉对应问题点
2) 代码层面 Code Review：
   - 可维护性（拆分是否合理、循环/复杂度是否可接受、命名一致性）
   - 回归风险（是否有测试/黄金用例覆盖；是否影响已有接口/文件边界）
   - 配置与版本治理（是否更新 schema / snapshot / 版本号）
3) 结果回填到 “codex comment”：
   - 通过：简要说明通过依据与核对点 → 在该行保持/改为 ✅
   - 需修正：逐条列出问题（文件:行号/模块/原因/建议修复方案）→ 将该行状态标为 ❓

输出格式建议（示例）：
- PASS:
  - [文件/模块] 具体核对点……（保持/改为 ✅）
- FIX:
  - [文件:行号] 问题描述 → 修复建议
  - [配置/阈值] 发现硬编码 → 建议改为 YAML 注入
  （将该行状态改为 ❓）

最后请给出「审核摘要」：
- 通过的行（含关键核对点）
- 需要修正的行（按严重程度排序）
- 共性风险与建议（如需要新增测试/拆分模块/补充文档）
